# AGENTS.md — chinatravel.world 项目协作规则

## 协作纪律

- **无明确命令，不改代码**：除非用户明确下达修改指令（如「改」「加」「删」「修复」「重构」「上线」等），否则不改动本项目中的任何代码、内容或配置文件，也不新建/覆盖文件。默认工作模式是**只探讨与规划**——输出分析、思路、方案与优先级建议，经用户确认后再执行。
- **提交需明示**：仅在用户明确要求时 commit / push；提交身份遵循 git-identity skill。

## 项目定位

- **China Travel Intelligence**：把中文互联网的真实旅行经验，筛选、交叉验证、翻译，组织成外国游客可直接使用的英文决策信息。中文攻略是 SEO 突破口与信息源，英文本地视角（local / authentic / hidden gems / real voices）是差异化。
- 不卖旅游服务、不接付费收录、不做「最佳景点」式拍脑壳榜单；独立性的正确表述是「不收钱」，不是「绝对中立」。

## SEO 战略铁律

- **关键词只从数据来**：优先级 GSC query→page 落点 > Google Trends > SERP 实查；不做无数据支撑的猜测选题。工具用法见 `ctg-guide-pipeline` skill。
- **中文是突破区，英文做超长尾**：中文页面快排名、高 CTR，值得做深；英文只争「城市 + 强意图修饰词」（local / authentic / traditional / itinerary / hidden / worth it / how-to），不硬拼 `chengdu food` 级泛词——那是权重起来之后的自然结果。
- **已排名页面不动地基**：中文标题与 URL 一律不改（西安三日游 #2 的教训价值）；英文标题可渐进植入目标主词（如补上缺失的 "Itinerary"），但不推翻重写。
- **新建页前查落点**：先确认 GSC 里目标词现在落在哪个页面。若已有页面承接该词且意图可分层（全景 vs 清单式），才建新页，且新旧页必须互链成簇；否则优先扩充现有页面。
- **节奏**：一次一批（3-5 页）→ 部署 → 观察 2-4 周 GSC → 复盘再下一批。禁止一次性铺几十上百个 AI 页面。
- **选题优先补「城市 × 意图」矩阵缺口**（street food / hot pot / itinerary / food tour / 避坑 / 熊猫 / 交通串联），而非重复写泛指南；跨城串联词（beijing to xian）是现成的关联信号来源。

## 内容编辑标准（三层证据）

- **Level 1 事实**：可验证的客观信息（价格、时刻、地址、机制）。
- **Level 2 中文网友共同观点**：引用 B 站热评必须带平台、赞数，英文页注明 *(translated from Chinese)*；强调 recurring opinions，不用单条评论定论。
- **Level 3 我们的判断**：明确以建议口吻给出（"If you prefer…"）。三层不得混写。
- 语气诚实、不浪漫化、给具体价格锚点与可执行规则（例：「本地人排的队就是最好的测评」「超过五块的蛋烘糕都是哈皮蛋烘糕」）。游客陷阱要敢点名，但以用户问题为主角、不攻击平台。
- B站 素材必须标注播放量快照日期；同一视频不在两个页面重复嵌入（新建组前先 `grep -rl "BV号" data/` 查重）。

## 交付标准

- 任何页面 / 数据改动后，跑 `npm run generate` 全量构建 + skill 中的渲染验证 grep 清单，全绿才算完成。
- GSC 是选题机器：每批评价看四类词（Top10 守住 / 11-30 冲刺 / 高曝光低 CTR 改标题描述 / 意外词建新页）。

## 环境备忘（Windows / Git Bash）

- `/tmp` 在 bash 与 node 中解析路径不同（`G:\tmp` vs 用户 TEMP），跨命令传递的临时文件放项目内路径。
- node 不能直接 import `.ts` 数据文件，语法校验依赖 `npm run generate`。
